[project]
name = "amazon-item-recommender"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    # For compatibility with recbole
    "pandas<=2.3.2",
    "numpy",
    "torch<=2.5",
    "matplotlib",
    "seaborn",
    "matplotlib-venn",
    "datasets",
    "ipykernel",
    "recbole",
    # For compatibility with recbole
    "kmeans-pytorch"
]
