[project]
name = "amazon-item-recommender"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pandas",
    "numpy",
    "matplotlib",
    "seaborn",
    "matplotlib-venn",
    "datasets",
    "ipykernel",
]
